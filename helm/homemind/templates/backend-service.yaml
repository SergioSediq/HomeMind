{{- if .Values.backend.enabled }}
{{- $annotations := dict -}}
{{- range $k, $v := .Values.global.cloud.service.annotations }}
{{- $_ := set $annotations $k $v -}}
{{- end }}
{{- range $k, $v := .Values.backend.service.annotations }}
{{- $_ := set $annotations $k $v -}}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "homemind.componentName" (dict "root" . "component" "backend") }}
  namespace: {{ include "homemind.namespace" . }}
  labels:
    {{- include "homemind.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
  {{- if gt (len $annotations) 0 }}
  annotations:
    {{- toYaml $annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "homemind.selectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
{{- end }}
