apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: homemind-backend-monitor
  namespace: homemind
  labels:
    app: homemind-backend
    release: prometheus
spec:
  selector:
    matchLabels:
      app: homemind-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: homemind-frontend-monitor
  namespace: homemind
  labels:
    app: homemind-frontend
    release: prometheus
spec:
  selector:
    matchLabels:
      app: homemind-frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
---
apiVersion: v1
kind: Service
metadata:
  name: homemind-backend-metrics
  namespace: homemind
  labels:
    app: homemind-backend
spec:
  selector:
    app: homemind-backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: homemind-frontend-metrics
  namespace: homemind
  labels:
    app: homemind-frontend
spec:
  selector:
    app: homemind-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
